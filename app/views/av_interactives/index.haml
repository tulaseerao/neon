- model_class = AvInteractive
.page-header
  %h1=t '.title', :default => model_class.model_name.human.pluralize.titleize
%table.table.table-striped
  %thead
    %tr
      %th= model_class.human_attribute_name(:id)
      %th= model_class.human_attribute_name(:brand)
      %th= model_class.human_attribute_name(:device_type)
      %th= model_class.human_attribute_name(:asset_tag)
      %th= model_class.human_attribute_name(:serial_no)
      %th= model_class.human_attribute_name(:created_at)
      %th=t '.actions', :default => t("helpers.actions")
  %tbody
    - @devices.each do |device|
      %tr
        %td= link_to device.id, av_interactive_path(device)
        %td= device.brand
        %td= device.device_type
        %td= device.asset_tag
        %td= device.serial_no
        %td=l device.created_at
        %td
          = link_to t('.edit', :default => t("helpers.links.edit")), edit_av_interactive_path(device), :class => 'btn btn-mini'
          = link_to t('.destroy', :default => t("helpers.links.destroy")), av_interactive_path(device), :method => :delete, :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) }, :class => 'btn btn-mini btn-danger'

= link_to t('.new', :default => t("helpers.links.new")), new_av_interactive_path, :class => 'btn btn-primary'
%br/
%br/